import { action } from '@storybook/addon-actions';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';

import SettingsDialog from '../components/SettingsDialog.js';
import ColorSettings from '../components/ColorSettings.js';
import CodeSettings from '../components/CodeSettings.js';
import ToyProblemSettings from '../components/ToyProblemSettings.js';

<Meta title="Settings Dialog Box" component={SettingsDialog} />

# Settings Dialog Box
### Button to trigger modal to open containing settings panes.

Use `SettingsDialog` to render a gear icon button that when clicked opens a modal view containing various settings panes.

<Preview>
  <Story name="Default Dialog View" height="50px">
    <SettingsDialog />
  </Story>
</Preview>

By default when passed a string through the `content` property, the card will render as black text on a white background.

## Props

<Props of={SettingsDialog} />

The SettingsDialog element has no properties that it takes in.

## Additional Stories

### Color Settings Sub-Element

<Preview>
  <Story name="Color Settings View" height="150px">
    <ColorSettings />
  </Story>
</Preview>

### Code Settings Sub-Element

<Preview>
  <Story name="Code Settings View" height="300px">
    <CodeSettings />
  </Story>
</Preview>

### Toy Problems Settings Sub-Element

<Preview>
  <Story name="Toy Problem Settings View" height="210px">
    <ToyProblemSettings />
  </Story>
</Preview>

No aditional stories for the SettingsDialog element.